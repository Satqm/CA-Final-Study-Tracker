<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CA Pro Tracker - License Generator</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #2c3e50 0%, #3498db 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .generator-container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            width: 100%;
            max-width: 600px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
        }
        
        .header {
            text-align: center;
            margin-bottom: 40px;
        }
        
        .header h1 {
            color: #2c3e50;
            margin-bottom: 10px;
            font-size: 28px;
        }
        
        .header p {
            color: #7f8c8d;
            font-size: 16px;
        }
        
        .form-group {
            margin-bottom: 25px;
        }
        
        label {
            display: block;
            margin-bottom: 10px;
            font-weight: 600;
            color: #2c3e50;
            font-size: 15px;
        }
        
        input, select {
            width: 100%;
            padding: 15px;
            border: 2px solid #e1e5e9;
            border-radius: 12px;
            font-size: 16px;
            transition: all 0.3s;
        }
        
        input:focus, select:focus {
            border-color: #3498db;
            outline: none;
            box-shadow: 0 0 0 4px rgba(52, 152, 219, 0.1);
        }
        
        .btn {
            width: 100%;
            padding: 16px;
            background: linear-gradient(135deg, #2c3e50 0%, #3498db 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }
        
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 30px rgba(52, 152, 219, 0.3);
        }
        
        .btn-accent {
            background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%) !important;
        }
        
        .btn-accent:hover {
            box-shadow: 0 15px 30px rgba(231, 76, 60, 0.3) !important;
        }
        
        .result-container {
            margin-top: 30px;
            padding: 25px;
            background: #f8f9fa;
            border-radius: 12px;
            display: none;
        }
        
        .result-container.show {
            display: block;
            animation: slideUp 0.5s ease;
        }
        
        @keyframes slideUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .license-details {
            background: white;
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 20px;
            border-left: 4px solid #3498db;
        }
        
        .license-details h4 {
            color: #2c3e50;
            margin-bottom: 15px;
            font-size: 18px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .license-details p {
            margin: 8px 0;
            color: #555;
            display: flex;
            justify-content: space-between;
        }
        
        .license-details .label {
            font-weight: 600;
            color: #2c3e50;
            min-width: 120px;
        }
        
        .license-key {
            background: white;
            padding: 20px;
            border-radius: 12px;
            font-family: 'Courier New', monospace;
            margin: 20px 0;
            word-break: break-all;
            border: 2px solid #3498db;
            font-size: 15px;
            line-height: 1.5;
            color: #2c3e50;
            text-align: center;
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        
        .copy-btn {
            padding: 14px 24px;
            background: #2ecc71;
            color: white;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            width: 100%;
            font-size: 16px;
        }
        
        .copy-btn:hover {
            background: #27ae60;
            transform: translateY(-2px);
        }
        
        .copy-btn.copied {
            background: #27ae60;
        }
        
        .security-notice {
            margin-top: 20px;
            padding: 20px;
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 10px;
            color: #856404;
            font-size: 14px;
        }
        
        .security-notice h4 {
            margin-bottom: 10px;
            color: #856404;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .security-notice ul {
            margin-top: 10px;
            margin-left: 20px;
        }
        
        .security-notice li {
            margin-bottom: 8px;
        }
        
        .admin-info {
            margin-top: 30px;
            text-align: center;
            font-size: 14px;
            color: #7f8c8d;
            border-top: 1px solid #eee;
            padding-top: 20px;
        }
        
        .test-license {
            margin-top: 20px;
            padding: 20px;
            background: #d4edda;
            border: 1px solid #c3e6cb;
            border-radius: 10px;
            color: #155724;
            font-size: 14px;
        }
        
        .test-license h4 {
            margin-bottom: 10px;
            color: #155724;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .test-license button {
            margin-top: 10px;
            padding: 12px 20px;
            background: #28a745;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            transition: all 0.3s;
        }
        
        .test-license button:hover {
            background: #218838;
            transform: translateY(-2px);
        }
        
        .action-buttons {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-top: 20px;
        }
        
        .message {
            padding: 15px;
            border-radius: 10px;
            margin-top: 15px;
            display: none;
        }
        
        .message.success {
            background: #d4edda;
            border: 1px solid #c3e6cb;
            color: #155724;
            display: block;
        }
        
        .message.error {
            background: #f8d7da;
            border: 1px solid #f5c6cb;
            color: #721c24;
            display: block;
        }
        
        @media (max-width: 768px) {
            .generator-container {
                padding: 25px;
            }
            
            .action-buttons {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="generator-container">
        <div class="header">
            <h1><i class="fas fa-key"></i> CA Pro Tracker License Generator</h1>
            <p>Admin Panel - Generate Premium License Keys</p>
        </div>
        
        <div class="form-group">
            <label for="customer-name"><i class="fas fa-user"></i> Customer Name</label>
            <input type="text" id="customer-name" placeholder="Enter customer's full name" value="Satyam Kumar">
        </div>
        
        <div class="form-group">
            <label for="license-type"><i class="fas fa-calendar-alt"></i> License Duration</label>
            <select id="license-type">
                <option value="7">7 Days (Trial)</option>
                <option value="30">30 Days (Monthly)</option>
                <option value="90">90 Days (Quarterly)</option>
                <option value="180">180 Days (Half Year)</option>
                <option value="365" selected>365 Days (Annual)</option>
                <option value="730">2 Years</option>
                <option value="9999">Lifetime</option>
            </select>
        </div>
        
        <div class="form-group">
            <label for="license-tier"><i class="fas fa-crown"></i> License Tier</label>
            <select id="license-tier">
                <option value="premium" selected>Premium</option>
                <option value="ultimate">Ultimate</option>
                <option value="enterprise">Enterprise</option>
            </select>
        </div>
        
        <button class="btn" id="generate-btn">
            <i class="fas fa-key"></i> Generate License Key
        </button>
        
        <div class="result-container" id="result-container">
            <h3><i class="fas fa-certificate"></i> Generated License Details</h3>
            
            <div class="license-details" id="license-details">
                <h4><i class="fas fa-info-circle"></i> License Information</h4>
                <p><span class="label">Customer:</span> <span id="customer-detail">-</span></p>
                <p><span class="label">Expiry Date:</span> <span id="expiry-detail">-</span></p>
                <p><span class="label">Duration:</span> <span id="duration-detail">-</span></p>
                <p><span class="label">Tier:</span> <span id="tier-detail">-</span></p>
                <p><span class="label">Generated:</span> <span id="generated-detail">-</span></p>
            </div>
            
            <div class="license-key" id="license-output">
                Your license key will appear here...
            </div>
            
            <div class="action-buttons">
                <button class="copy-btn" id="copy-btn">
                    <i class="fas fa-copy"></i> Copy License Key
                </button>
                <button class="btn btn-accent" id="send-btn">
                    <i class="fas fa-paper-plane"></i> Send via WhatsApp
                </button>
            </div>
            
            <div class="message" id="message"></div>
            
            <div class="security-notice">
                <h4><i class="fas fa-shield-alt"></i> Security Instructions:</h4>
                <ul>
                    <li>License format: <strong>CA-PRO-PREMIUM-XXXXXXX</strong></li>
                    <li>Share this key only with authorized customer</li>
                    <li>Key contains encrypted customer data</li>
                    <li>Store this information securely</li>
                    <li>Key expires automatically on the expiry date</li>
                    <li>Double encryption ensures maximum security</li>
                </ul>
            </div>
        </div>
        
        <div class="test-license">
            <h4><i class="fas fa-vial"></i> Test License Generation</h4>
            <p>Generate test licenses for quality assurance</p>
            <div class="action-buttons">
                <button id="test-btn-premium">
                    <i class="fas fa-bolt"></i> Test Premium License
                </button>
                <button id="test-btn-ultimate">
                    <i class="fas fa-crown"></i> Test Ultimate License
                </button>
            </div>
        </div>
        
        <div class="admin-info">
            <p><i class="fas fa-copyright"></i> 2026 Satyam Kumar • CA Final Pro Tracker</p>
            <p style="margin-top: 5px;"><i class="fas fa-phone"></i> Contact: +91 91423 00129</p>
            <p style="margin-top: 5px;"><i class="fas fa-globe"></i> Website: satqm.github.io/Portfolio-/</p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/crypto-js@4.1.1/crypto-js.min.js"></script>
    <script>
        // USE THE SAME KEYS AS IN THE MAIN APP
        const MASTER_KEY = "S@tyamCA2026@Premium";
        const BACKUP_KEY = "RadhaKrishna@2026";
        
        // DOM Elements
        const generateBtn = document.getElementById('generate-btn');
        const copyBtn = document.getElementById('copy-btn');
        const sendBtn = document.getElementById('send-btn');
        const testBtnPremium = document.getElementById('test-btn-premium');
        const testBtnUltimate = document.getElementById('test-btn-ultimate');
        const customerNameInput = document.getElementById('customer-name');
        const licenseTypeSelect = document.getElementById('license-type');
        const licenseTierSelect = document.getElementById('license-tier');
        const resultContainer = document.getElementById('result-container');
        const licenseOutput = document.getElementById('license-output');
        const customerDetail = document.getElementById('customer-detail');
        const expiryDetail = document.getElementById('expiry-detail');
        const durationDetail = document.getElementById('duration-detail');
        const tierDetail = document.getElementById('tier-detail');
        const generatedDetail = document.getElementById('generated-detail');
        const messageEl = document.getElementById('message');
        
        // Event Listeners
        generateBtn.addEventListener('click', generateLicense);
        copyBtn.addEventListener('click', copyLicense);
        sendBtn.addEventListener('click', sendViaWhatsApp);
        testBtnPremium.addEventListener('click', () => generateTestLicense('premium'));
        testBtnUltimate.addEventListener('click', () => generateTestLicense('ultimate'));
        
        // Generate device ID (same as in main app)
        function generateDeviceId() {
            const fingerprint = [
                navigator.userAgent,
                navigator.platform,
                screen.width,
                screen.height,
                screen.colorDepth,
                new Date().getTimezoneOffset()
            ].join('|');
            
            return CryptoJS.MD5(fingerprint).toString();
        }
        
        function generateLicense() {
            const name = customerNameInput.value.trim();
            const days = parseInt(licenseTypeSelect.value);
            const tier = licenseTierSelect.value;
            
            if (!name) {
                showMessage('❌ Please enter customer name', 'error');
                customerNameInput.focus();
                return;
            }
            
            if (days <= 0) {
                showMessage('❌ Please select valid license duration', 'error');
                return;
            }
            
            const expiryDate = new Date();
            expiryDate.setDate(expiryDate.getDate() + days);
            
            // Create license data structure EXACTLY as main app expects
            const licenseData = {
                name: name.trim().toUpperCase(),
                expiry: expiryDate.toISOString(),
                generated: new Date().toISOString(),
                type: tier,
                version: "4.0",
                deviceId: "" // Empty - will be set when activated
            };
            
            console.log("License Data:", licenseData);
            
            try {
                // DOUBLE ENCRYPTION - SAME AS MAIN APP
                const jsonString = JSON.stringify(licenseData);
                
                // First encryption with MASTER_KEY
                const encrypted1 = CryptoJS.AES.encrypt(jsonString, MASTER_KEY).toString();
                
                // Second encryption with BACKUP_KEY
                const encrypted2 = CryptoJS.AES.encrypt(encrypted1, BACKUP_KEY).toString();
                
                // Base64 encode
                const base64 = btoa(encrypted2);
                
                // Format: CA-PRO-{tier}-{base64}
                const licenseKey = `CA-PRO-${tier.toUpperCase()}-${base64}`;
                
                // Update UI
                licenseOutput.textContent = licenseKey;
                customerDetail.textContent = name;
                expiryDetail.textContent = formatDate(expiryDate);
                durationDetail.textContent = days === 9999 ? "Lifetime" : `${days} days`;
                tierDetail.textContent = tier.charAt(0).toUpperCase() + tier.slice(1);
                generatedDetail.textContent = new Date().toLocaleString();
                
                // Show result
                resultContainer.classList.add('show');
                
                // Scroll to result
                resultContainer.scrollIntoView({ behavior: 'smooth' });
                
                // Auto-copy to clipboard
                setTimeout(() => {
                    copyToClipboard(licenseKey);
                    showMessage('✅ License generated and copied to clipboard!', 'success');
                }, 300);
                
            } catch (error) {
                console.error("License generation error:", error);
                showMessage(`❌ Error generating license: ${error.message}`, 'error');
            }
        }
        
        function generateTestLicense(tier) {
            // Set test values
            customerNameInput.value = "Test User";
            licenseTypeSelect.value = "30";
            licenseTierSelect.value = tier;
            
            // Generate license
            generateLicense();
        }
        
        function copyLicense() {
            const licenseText = licenseOutput.textContent;
            
            if (licenseText.includes("Your license key")) {
                showMessage("❌ Please generate a license first", 'error');
                return;
            }
            
            copyToClipboard(licenseText);
        }
        
        function copyToClipboard(text) {
            navigator.clipboard.writeText(text)
                .then(() => {
                    // Visual feedback
                    const btn = copyBtn;
                    const originalHTML = btn.innerHTML;
                    btn.innerHTML = '<i class="fas fa-check"></i> Copied!';
                    btn.classList.add('copied');
                    
                    // Reset button after 2 seconds
                    setTimeout(() => {
                        btn.innerHTML = originalHTML;
                        btn.classList.remove('copied');
                    }, 2000);
                    
                    console.log("✅ License copied to clipboard:", text.substring(0, 40) + "...");
                })
                .catch(err => {
                    console.error("Copy failed:", err);
                    
                    // Fallback method
                    const textArea = document.createElement("textarea");
                    textArea.value = text;
                    document.body.appendChild(textArea);
                    textArea.select();
                    document.execCommand("copy");
                    document.body.removeChild(textArea);
                    
                    // Show feedback
                    const btn = copyBtn;
                    const originalHTML = btn.innerHTML;
                    btn.innerHTML = '<i class="fas fa-check"></i> Copied!';
                    btn.classList.add('copied');
                    
                    setTimeout(() => {
                        btn.innerHTML = originalHTML;
                        btn.classList.remove('copied');
                    }, 2000);
                });
        }
        
        function sendViaWhatsApp() {
            const licenseText = licenseOutput.textContent;
            
            if (licenseText.includes("Your license key")) {
                showMessage("❌ Please generate a license first", 'error');
                return;
            }
            
            const name = customerNameInput.value.trim();
            const phone = "+919142300129"; // Your WhatsApp number
            
            // Format message
            const message = encodeURIComponent(
                `Hello ${name},\n\n` +
                `Your CA Final Pro Tracker license has been generated:\n\n` +
                `🔑 License Key:\n${licenseText}\n\n` +
                `📅 Expiry: ${expiryDetail.textContent}\n` +
                `📊 Tier: ${tierDetail.textContent}\n\n` +
                `Instructions:\n` +
                `1. Open CA Final Pro Tracker\n` +
                `2. Paste the license key in the activation field\n` +
                `3. Click "Activate Premium License"\n\n` +
                `If you face any issues, please contact us.\n\n` +
                `Best regards,\nSatyam Kumar`
            );
            
            // Open WhatsApp
            window.open(`https://wa.me/${phone}?text=${message}`, '_blank');
            
            showMessage('✅ WhatsApp opened with license details', 'success');
        }
        
        function formatDate(date) {
            return date.toLocaleDateString('en-US', {
                year: 'numeric',
                month: 'long',
                day: 'numeric',
                weekday: 'long'
            });
        }
        
        function showMessage(text, type) {
            messageEl.textContent = text;
            messageEl.className = `message ${type}`;
            
            // Hide after 5 seconds
            setTimeout(() => {
                messageEl.style.opacity = '0';
                setTimeout(() => {
                    messageEl.style.display = 'none';
                    messageEl.style.opacity = '1';
                }, 300);
            }, 5000);
        }
        
        // Keyboard shortcuts
        document.addEventListener('keydown', (e) => {
            // Ctrl+G to generate
            if (e.ctrlKey && e.key === 'g') {
                e.preventDefault();
                generateLicense();
            }
            // Ctrl+C to copy
            if (e.ctrlKey && e.key === 'c' && resultContainer.classList.contains('show')) {
                e.preventDefault();
                copyLicense();
            }
            // Ctrl+W to send via WhatsApp
            if (e.ctrlKey && e.key === 'w' && resultContainer.classList.contains('show')) {
                e.preventDefault();
                sendViaWhatsApp();
            }
        });
        
        // Add keyboard shortcut hint
        console.log(`
        🚀 CA Pro Tracker License Generator v4.0
        ========================================
        • Ctrl+G → Generate License
        • Ctrl+C → Copy License Key (when shown)
        • Ctrl+W → Send via WhatsApp (when shown)
        
        Encryption Keys:
        • Master: ${MASTER_KEY}
        • Backup: ${BACKUP_KEY}
        
        Format: CA-PRO-{TIER}-{Base64EncryptedData}
        Tiers: PREMIUM, ULTIMATE, ENTERPRISE
        `);
        
        // Generate a sample license on load for testing
        window.addEventListener('DOMContentLoaded', () => {
            // Optional: Uncomment to auto-generate a test license on load
            // generateTestLicense('premium');
        });
    </script>
</body>
              </html>
